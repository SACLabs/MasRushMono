# The receiver receive Code and other stuff from env

def test_mas_to_env(src_file: "demand_example_src_uri"):
    # assert  results_after_run == "cpofile_json", 'pytest_json'
    assert file_name == "xxx", task_id is not None